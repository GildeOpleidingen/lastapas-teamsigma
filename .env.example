DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Used to sign table/admin cookies. Generate with: openssl rand -base64 32
APP_SECRET=

# Admin table dashboard
ADMIN_PASSCODE=
# Optional; falls back to APP_SECRET when omitted
ADMIN_SESSION_SECRET=

# Pusher Channels
PUSHER_APP_ID=
PUSHER_SECRET=
NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=
